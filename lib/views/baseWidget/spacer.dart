// ignore_for_file: use_key_in_widget_constructors
import 'package:flutter/material.dart';

class WSpacer extends SizedBox {
  const WSpacer(double size) : super(width: size);
}

class HSpacer extends SizedBox {
  const HSpacer(double size) : super(height: size);
}
